name: "CodeQL"

on:
  push:
    branches:
      - main
      - 'feature/**'
      - 'release/**'
      - 'hotfix/**'
  schedule:
    - cron: '29 18 * * 2'
    jobs:
      env:
        uses: devxp-tech/.github/.github/workflows/codeql.yaml@main
        permissions:
          actions: read
          contents: read
        with:
          languague: go