apiVersion: v1
kind: Service
metadata:
  name: teste-loki
  labels:
    app: teste-loki
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: teste-loki
